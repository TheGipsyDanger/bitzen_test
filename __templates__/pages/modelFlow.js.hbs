import { I{{pascalCase name}} } from '~/pages/{{pascalCase navigator}}Navigator/{{pascalCase name}}/{{pascalCase name}}.types'

export const use{{ pascalCase name }} = (props: I{{pascalCase name}}.IModelProps ): I{{pascalCase name}}.IModel => {
  return {}
}